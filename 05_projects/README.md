# Projects

Create one folder per assessed task or dissertation, using lowercase snake case, for example `mgt601_strategy_report` or `dissertation_2027`.

Start each folder by copying `04_templates/Project_Context.md`. Create `private/`, `drafts/`, `feedback/`, or `data/` only when required; they are ignored by Git by default.

